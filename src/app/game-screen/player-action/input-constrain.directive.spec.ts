import { InputConstrainDirective } from './input-constrain.directive';

describe('InputConstrainDirective', () => {
  it('should create an instance', () => {
    const directive = new InputConstrainDirective();
    expect(directive).toBeTruthy();
  });
});
